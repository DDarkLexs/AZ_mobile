export enum Routes {
  LOGIN = 'ENTRADA',
  REGISTER = 'REGISTRO',
  HOME = 'PRINCIPAL',
  INVENTARIO = 'INVENTÁRIO',
}
export enum Cargo {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum Sexo {
  MASCULINO = 'MASCULINO',
  FEMININO = 'FEMININO',
}
